import { styled } from '@/styles'

export const TableFoodsContainer = styled('table', {
  border: '1px solid $white',

  'th, td': {
    border: '1px solid $white',
    fontSize: '14px',
    padding: '5px',
  },
})
